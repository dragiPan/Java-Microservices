server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "3.0.5"

accounts:
  message: "Welcome to Bank accounts Microservice"
  contactDetails:
    name: "Dragi Pantović - Developer"
    email: "dtm.pantovic@gmail.com"
  onCallSupport:
    - (+91) 9872109455
    - (+91) 9884567822
