spring:
  application:
    name: "spring-cloud"
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/dragiPan/Spring-Config-Files"
          bra

server:
  port: 8071