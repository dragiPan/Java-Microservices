spring:
  application:
    name: "spring-cloud"
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/dragiPan/Spring-Config-Files.git"
          default-label: main

logging:
  level:
    org.springframework.cloud.config: DEBUG

server:
  port: 8071